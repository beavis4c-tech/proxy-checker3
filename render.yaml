services:
  - type: web
    name: proxy-checker
    env: python
    region: oregon
    buildCommand: pip install -r cloud_checker/requirements_cloud.txt
    startCommand: ./start.sh
    plan: free
    envVars:
      - key: PYTHONUNBUFFERED
        value: 1